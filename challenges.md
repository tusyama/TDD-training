Отличная идея — практиковаться в TDD именно на небольших, но «жёстко определённых» задачах 👍
Ниже я подготовил несколько тестовых заданий разной сложности специально под JavaScript и стиль разработки через тесты.

Каждое задание описано так, чтобы:

- сначала можно было написать тесты,
- поведение было чётко определено,
- было много крайних случаев для практики.

---

## 🧪 Разминка — `Queue`

**Цель** — реализовать структуру данных Очередь (FIFO)

Реализуй класс:

```
Queue
 ├ enqueue(value)
 ├ dequeue()
 ├ peek()
 ├ size()
 ├ isEmpty()
```

### Напиши тесты для следующих сценариев

- новая очередь пуста
- `enqueue` увеличивает размер
- `dequeue` возвращает первый добавленный элемент
- после `dequeue` элементы сдвигаются
- `peek` возвращает элемент, но не удаляет
- `dequeue` из пустой очереди → `null`
- `peek` из пустой очереди → `null`

**Дополнительный уровень**

- очередь должна корректно работать с:

  - числами
  - строками
  - объектами

---

## 🎯 Уровень 2 — `LRUCache`

Класс:

```
LRUCache(capacity)
 ├ get(key)
 ├ set(key, value)
```

### Требования для тестов

- при добавлении элементов ёмкость не превышается
- при переполнении удаляется самый редко использованный ключ
- `get` обновляет «свежесть»
- `get` неизвестного ключа → `undefined`
- значение ключа перезаписывается корректно

**Пример сценария**

1. `set(a,1)`
2. `set(b,2)`
3. `get(a)` → теперь `a` самый свежий
4. `set(c,3)` при capacity=2

ожидаем — удалён `b`

---

## 🧩 Уровень 3 — Конвертер римских чисел

Функция:

```
romanToInt(str)
```

### Напиши тесты для

- `III → 3`
- `IV → 4`
- `IX → 9`
- `LVIII → 58`
- `MCMXCIV → 1994`
- lower-case строки
- некорректные символы → ошибка

**Бонус-итерация (TDD-style)**

Добавить обратную функцию:

```
intToRoman(n)
```

---

## 🎳 Ката — Подсчёт очков в боулинге

Функция:

```
score(frames)
```

где frames — массив бросков.

### Обязательные тестовые сценарии

- все промахи → `0`
- все единицы → `20`
- один spare даёт бонус следующего броска
- один strike даёт бонус следующих двух бросков
- игра из 12 strike → `300`
- корректная обработка 10-го фрейма

Эта задача специально хороша для **итеративного TDD**.

---

## ⚡ Уровень 4 — `EventEmitter`

Класс:

```
EventEmitter
 ├ on(event, handler)
 ├ off(event, handler)
 ├ once(event, handler)
 ├ emit(event, ...args)
```

### Напиши тесты для

- вызов одного обработчика
- передача аргументов
- несколько обработчиков на событие
- `off` удаляет конкретный обработчик
- `once` вызывается только один раз
- вызов несуществующего события не падает

**Бонус**

- порядок выполнения обработчиков сохраняется

---

## 🛠 Рекомендации по процессу TDD

Для каждого задания попробуй придерживаться цикла:

1️⃣ Написать один минимальный тест
2️⃣ Убедиться, что он падает
3️⃣ Реализовать минимальный код
4️⃣ Рефакторинг
5️⃣ Повторить
